apiVersion: v1
kind: Service
metadata:
  # Bu isim çok önemli - user-service, PostgreSQL'e artık
  # "user-postgres" ismiyle ulaşacak (localhost değil!)
  name: user-postgres
spec:
  selector:
    app: user-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
